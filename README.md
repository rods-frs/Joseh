Joseh is a AI agent that for now will work as an "Alexa" or something like this, so don`t expect to make GPT like questions.

==Joseh was only tested on ArchLinux, I don`t know if it runs on other distros but its certain that is a Linux-only program.

He uses the Spacy to identify the verb and the object of the user input to execute the function that will do what the user wants. For now it only has the OPEN function.
